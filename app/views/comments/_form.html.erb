
<%= form_with(model: [ @post, @post.comments.build ], local: true) do |form| %>
  <%= form.text_area :message, placeholder: 'Message', class: "form-control" %>
  <%= form.submit "Add comment", class: "btn btn-primary" %>
<% end %>